# TODO

- [ ] Create career slug page at `app/career/[slug]/page.tsx`
- [ ] Update `app/career/page.tsx` to add `slug` to each job and link the “Read More” button to `/career/[slug]`
- [ ] Run `npm run build` (or `next lint`) to verify routing/TS
